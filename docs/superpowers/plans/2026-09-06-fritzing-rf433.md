# Peças Fritzing RF 433 MHz — plano de implementação

> **Para quem executa:** SUB-SKILL OBRIGATÓRIA — usar `superpowers:subagent-driven-development`
> ou `superpowers:executing-plans`. Os passos estão em caixinhas `- [ ]`.

**Objetivo:** entregar duas peças Fritzing instaláveis — transmissor FS1000A e receptor
MX-05V de 433 MHz — com as quatro vistas, validadas por teste automatizado e conferidas
a olho antes de serem dadas por prontas.

**Arquitetura:** FZP e SVG escritos à mão em `fritzing/`, um validador em Python que roda
no pytest do repositório, e um empacotador que zipa cada peça no formato `.fzpz`. O
desenho em si não tem teste possível — para ele existe uma folha de contato renderizada
pelo `previa.ps1` que já está no repositório.

**Ferramentas:** Python 3.12, pytest, PyYAML (já em `requisitos.txt`), `zipfile` e
`xml.etree` da biblioteca padrão, `previa.ps1` para captura por CDP, e o `fzp_checker.py`
que vem com o Fritzing em
`~/AppData/Local/Programs/Fritzing/fritzing-parts/fzp_checker.py`.

**Spec:** `docs/superpowers/specs/2026-09-06-fritzing-rf433-design.md`

---

## Sistemas de coordenadas

Cada vista tem o seu, copiado de peças do core. Errar isto faz a peça sair com tamanho
físico errado e não encaixar no passo de 0,1 polegada — é o defeito mais caro do trabalho.

| vista | unidade do viewBox | passo de 0,1" vale | 1 mm vale |
|---|---|---|---|
| breadboard | 72 por polegada | 7,2 u | 2,834646 u |
| schematic | 72 por polegada | 7,2 u | 2,834646 u |
| pcb | **1000 por polegada** | 100 u | 39,3701 u |
| icon | 72 por polegada | — | 2,834646 u |

`width` e `height` sempre em polegadas, no elemento `<svg>`. Sem isso o Fritzing adivinha
o DPI.

## Idiomas de conector, por vista

**breadboard** — o pino metálico visível É o conector, sem elemento invisível por cima:

```xml
<rect id="connector0pin" x="8.929" y="53.858" width="1.814" height="10.205"
      fill="#8c8c8c" stroke="none"/>
```

**schematic** — retângulo de 0,7 u de altura fazendo as vezes da linha do pino:

```xml
<rect id="connector0pin" connectorName="DATA" x="0" y="28.45" width="14.4" height="0.7"
      fill="#787878" stroke="none"/>
```

**pcb** — furo THT, `copper0` aninhado dentro de `copper1` para o mesmo id servir às duas
camadas:

```xml
<g id="copper1">
  <g id="copper0">
    <circle id="connector0pin" cx="136.6" cy="696.8" r="27.5"
            fill="none" stroke="rgb(255, 191, 0)" stroke-width="20"/>
  </g>
</g>
```

O furo sai com 35 mils e o anel com 75 mils, que é o padrão dos DIP do core.

---

## Estrutura de arquivos

- `fritzing/validar.py` — carrega uma pasta de peça e devolve lista de problemas. Sem
  efeito colateral, sem I/O além de ler arquivo. É o que os testes exercitam.
- `fritzing/empacotar.py` — zipa uma pasta de peça em `.fzpz`. Depende de `validar.py`
  e se recusa a empacotar peça com problema.
- `fritzing/rf433-tx/part.fzp` + `fritzing/rf433-tx/svg/{breadboard,schematic,pcb,icon}/`
- `fritzing/rf433-rx/…` idem
- `fritzing/previa.html` — folha de contato das oito SVGs
- `fritzing/README.md` — como instalar a peça no Fritzing
- `testes/test_fritzing.py` — testes do validador e das duas peças reais

`validar.py` e `empacotar.py` ficam separados porque validação é lógica pura e testável
com arquivos de mentira em `tmp_path`, e empacotamento mexe em zip. Juntos, testar um
obrigaria a criar zip para o outro.

---

## Task 1: validador de peça

**Arquivos:**
- Criar: `fritzing/validar.py`
- Criar: `testes/test_fritzing.py`

O validador responde uma pergunta: esta pasta vira uma peça que o Fritzing abre sem
reclamar? Ele confere o que dá para conferir por leitura — arquivo citado que não existe,
conector do FZP sem elemento correspondente no SVG, passo fora da grade, fonte proibida,
propriedade vazia, barramento apontando para conector inexistente.

- [ ] **Passo 1: escrever os testes que falham**

```python
"""Testes das pecas Fritzing: o validador e as duas pecas reais."""
import sys
import zipfile
from pathlib import Path
from xml.etree import ElementTree

import pytest

RAIZ = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(RAIZ / "fritzing"))

import empacotar  # noqa: E402
import validar  # noqa: E402


def escrever(caminho, texto):
    caminho.parent.mkdir(parents=True, exist_ok=True)
    caminho.write_text(texto, encoding="utf-8")


FZP_MINIMO = """<?xml version="1.0" encoding="UTF-8"?>
<module moduleId="peca-teste">
 <version>1</version>
 <title>Peca de teste</title>
 <description>Peca de teste</description>
 <tags><tag>teste</tag></tags>
 <properties><property name="family">teste</property></properties>
 <views>
  <breadboardView><layers image="breadboard/bb.svg"><layer layerId="breadboard"/></layers></breadboardView>
  <schematicView><layers image="schematic/sc.svg"><layer layerId="schematic"/></layers></schematicView>
  <pcbView><layers image="pcb/pcb.svg"><layer layerId="copper1"/><layer layerId="copper0"/><layer layerId="silkscreen"/></layers></pcbView>
  <iconView><layers image="icon/ic.svg"><layer layerId="icon"/></layers></iconView>
 </views>
 <connectors>
  <connector type="male" name="A" id="connector0">
   <description>A</description>
   <views>
    <breadboardView><p svgId="connector0pin" layer="breadboard"/></breadboardView>
    <schematicView><p svgId="connector0pin" layer="schematic"/></schematicView>
    <pcbView><p svgId="connector0pin" layer="copper0"/><p svgId="connector0pin" layer="copper1"/></pcbView>
   </views>
  </connector>
  <connector type="male" name="B" id="connector1">
   <description>B</description>
   <views>
    <breadboardView><p svgId="connector1pin" layer="breadboard"/></breadboardView>
    <schematicView><p svgId="connector1pin" layer="schematic"/></schematicView>
    <pcbView><p svgId="connector1pin" layer="copper0"/><p svgId="connector1pin" layer="copper1"/></pcbView>
   </views>
  </connector>
 </connectors>
 <buses/>
</module>
"""

BB_MINIMO = """<svg xmlns="http://www.w3.org/2000/svg" width="0.5in" height="0.5in" viewBox="0 0 36 36">
 <g id="breadboard">
  <rect id="connector0pin" x="7.1" y="20" width="1.8" height="10" fill="#8c8c8c"/>
  <rect id="connector1pin" x="14.3" y="20" width="1.8" height="10" fill="#8c8c8c"/>
 </g>
</svg>
"""

SC_MINIMO = """<svg xmlns="http://www.w3.org/2000/svg" width="0.5in" height="0.5in" viewBox="0 0 36 36">
 <g id="schematic">
  <rect id="connector0pin" x="0" y="7.2" width="14.4" height="0.7" fill="#787878"/>
  <rect id="connector1pin" x="0" y="14.4" width="14.4" height="0.7" fill="#787878"/>
 </g>
</svg>
"""

PCB_MINIMO = """<svg xmlns="http://www.w3.org/2000/svg" width="0.5in" height="0.5in" viewBox="0 0 500 500">
 <g id="copper1"><g id="copper0">
  <circle id="connector0pin" cx="100" cy="100" r="27.5" fill="none" stroke="rgb(255, 191, 0)" stroke-width="20"/>
  <circle id="connector1pin" cx="200" cy="100" r="27.5" fill="none" stroke="rgb(255, 191, 0)" stroke-width="20"/>
 </g></g>
 <g id="silkscreen"><rect x="10" y="10" width="480" height="480" fill="none" stroke="#f0f0f0" stroke-width="10"/></g>
</svg>
"""

IC_MINIMO = """<svg xmlns="http://www.w3.org/2000/svg" width="0.3in" height="0.3in" viewBox="0 0 21.6 21.6">
 <g id="icon"><rect x="0" y="0" width="21.6" height="21.6" fill="#1f7a34"/></g>
</svg>
"""


@pytest.fixture
def peca(tmp_path):
    """Peca minima e valida. Cada teste estraga um pedaco e confere a queixa."""
    escrever(tmp_path / "part.fzp", FZP_MINIMO)
    escrever(tmp_path / "svg" / "breadboard" / "bb.svg", BB_MINIMO)
    escrever(tmp_path / "svg" / "schematic" / "sc.svg", SC_MINIMO)
    escrever(tmp_path / "svg" / "pcb" / "pcb.svg", PCB_MINIMO)
    escrever(tmp_path / "svg" / "icon" / "ic.svg", IC_MINIMO)
    return tmp_path


def test_peca_minima_nao_tem_queixa(peca):
    assert validar.problemas(peca) == []


def test_acusa_svg_que_o_fzp_cita_e_nao_existe(peca):
    (peca / "svg" / "icon" / "ic.svg").unlink()
    assert any("icon/ic.svg" in p for p in validar.problemas(peca))


def test_acusa_conector_sem_elemento_no_svg(peca):
    texto = BB_MINIMO.replace('id="connector1pin"', 'id="connector9pin"')
    escrever(peca / "svg" / "breadboard" / "bb.svg", texto)
    assert any("connector1pin" in p and "breadboard" in p for p in validar.problemas(peca))


def test_acusa_svg_sem_viewbox(peca):
    texto = BB_MINIMO.replace('viewBox="0 0 36 36"', "")
    escrever(peca / "svg" / "breadboard" / "bb.svg", texto)
    assert any("viewBox" in p for p in validar.problemas(peca))


def test_acusa_largura_sem_unidade_fisica(peca):
    texto = BB_MINIMO.replace('width="0.5in"', 'width="36px"')
    escrever(peca / "svg" / "breadboard" / "bb.svg", texto)
    assert any("width" in p for p in validar.problemas(peca))


def test_acusa_fonte_fora_da_lista(peca):
    texto = SC_MINIMO.replace(
        "</g>", '<text x="1" y="1" font-family="Arial" font-size="5">x</text></g>'
    )
    escrever(peca / "svg" / "schematic" / "sc.svg", texto)
    assert any("Arial" in p for p in validar.problemas(peca))


def test_aceita_noto_sans(peca):
    texto = SC_MINIMO.replace(
        "</g>", '<text x="1" y="1" font-family="Noto Sans" font-size="5">x</text></g>'
    )
    escrever(peca / "svg" / "schematic" / "sc.svg", texto)
    assert validar.problemas(peca) == []


def test_acusa_id_duplicado_no_mesmo_svg(peca):
    texto = BB_MINIMO.replace('id="connector1pin"', 'id="connector0pin"')
    escrever(peca / "svg" / "breadboard" / "bb.svg", texto)
    assert any("duplicado" in p for p in validar.problemas(peca))


def test_acusa_propriedade_vazia(peca):
    texto = FZP_MINIMO.replace(
        '<property name="family">teste</property>',
        '<property name="family">teste</property><property name="mpn"></property>',
    )
    escrever(peca / "part.fzp", texto)
    assert any("mpn" in p for p in validar.problemas(peca))


def test_acusa_barramento_com_conector_inexistente(peca):
    texto = FZP_MINIMO.replace(
        "<buses/>",
        '<buses><bus id="dados"><nodeMember connectorId="connector7"/></bus></buses>',
    )
    escrever(peca / "part.fzp", texto)
    assert any("connector7" in p for p in validar.problemas(peca))


def test_acusa_terminalid_declarado(peca):
    """A 1.0.3 calcula o terminal sozinho; declarar a mao e a causa mais comum
    de terminal invisivel, que e o defeito classico de peca caseira."""
    texto = FZP_MINIMO.replace(
        '<p svgId="connector0pin" layer="schematic"/>',
        '<p svgId="connector0pin" terminalId="connector0terminal" layer="schematic"/>',
    )
    escrever(peca / "part.fzp", texto)
    assert any("terminalId" in p for p in validar.problemas(peca))


def test_acusa_passo_fora_da_grade_no_breadboard(peca):
    """7,2 unidades e o passo de 0,1 polegada. Fora disso a peca nao encaixa."""
    texto = BB_MINIMO.replace('x="14.3"', 'x="13.0"')
    escrever(peca / "svg" / "breadboard" / "bb.svg", texto)
    assert any("passo" in p for p in validar.problemas(peca))
```

- [ ] **Passo 2: rodar e confirmar que falha**

Comando: `cd /c/HENRIQUE/Claude/Arduino && python -m pytest testes/test_fritzing.py -q`
Esperado: erro de coleta, `ModuleNotFoundError: No module named 'empacotar'`.

- [ ] **Passo 3: escrever `fritzing/validar.py`**

```python
"""Confere se uma pasta de peca Fritzing vira peca que o Fritzing abre sem reclamar.

Confere so o que da para saber por leitura: arquivo citado que nao existe, conector
sem elemento no SVG, passo fora da grade de 0,1 polegada, fonte proibida, id repetido,
propriedade vazia, barramento apontando para conector que nao existe, e terminalId
declarado a mao.

As regras saem de fritzing-parts/scripts/checks/svg_checkers.py e explain_errors.md,
do proprio Fritzing. O desenho em si nao tem conferencia automatica: para isso existe
a folha de contato em previa.html.
"""
from pathlib import Path
from xml.etree import ElementTree

SVG = "{http://www.w3.org/2000/svg}"

# svg_checkers.py, VALID_FONTS. Noto Sans nas vistas de desenho, OCR na serigrafia.
FONTES = {"Noto Sans", "OCR-Fritzing-mono", "Droid Sans", "Droid Sans Mono", "OCRA"}

# 72 unidades por polegada no breadboard, entao o passo de 0,1" e 7,2.
PASSO = 7.2
FOLGA = 0.05

VISTAS = {
    "breadboardView": "breadboard",
    "schematicView": "schematic",
    "pcbView": "pcb",
    "iconView": "icon",
}


def problemas(pasta):
    """Lista de queixas, em portugues, sobre a peca em `pasta`. Vazia significa boa."""
    pasta = Path(pasta)
    fzp = pasta / "part.fzp"
    if not fzp.exists():
        return [f"{pasta.name}: nao tem part.fzp"]

    raiz = ElementTree.parse(fzp).getroot()
    achados = []
    achados += _conferir_propriedades(raiz)

    ids_declarados = {c.get("id") for c in raiz.iter("connector")}
    achados += _conferir_barramentos(raiz, ids_declarados)
    achados += _conferir_terminais(raiz)

    for tag, vista in VISTAS.items():
        no = raiz.find(f".//{tag}/layers")
        if no is None:
            achados.append(f"{vista}: o FZP nao declara a vista")
            continue
        arquivo = pasta / "svg" / no.get("image")
        if not arquivo.exists():
            achados.append(f"{vista}: o FZP cita {no.get('image')}, que nao existe")
            continue
        achados += _conferir_svg(arquivo, vista)
        achados += _conferir_conectores(raiz, arquivo, tag, vista)

    return achados


def _conferir_propriedades(raiz):
    achados = []
    for prop in raiz.iter("property"):
        if not (prop.text or "").strip():
            achados.append(f"propriedade '{prop.get('name')}' esta vazia")
    return achados


def _conferir_barramentos(raiz, ids_declarados):
    achados = []
    for membro in raiz.iter("nodeMember"):
        alvo = membro.get("connectorId")
        if alvo not in ids_declarados:
            achados.append(f"barramento aponta para {alvo}, que nao e conector da peca")
    return achados


def _conferir_terminais(raiz):
    achados = []
    for p in raiz.iter("p"):
        if p.get("terminalId"):
            achados.append(
                f"{p.get('svgId')} declara terminalId; a 1.0.3 calcula o terminal "
                "sozinho e declarar a mao produz terminal invisivel"
            )
    return achados


def _conferir_svg(arquivo, vista):
    achados = []
    raiz = ElementTree.parse(arquivo).getroot()

    if not raiz.get("viewBox"):
        achados.append(f"{vista}: o SVG nao tem viewBox; o Fritzing adivinharia o DPI")
    for atributo in ("width", "height"):
        valor = raiz.get(atributo) or ""
        if not (valor.endswith("in") or valor.endswith("mm")):
            achados.append(
                f"{vista}: {atributo}='{valor}' nao esta em polegada nem milimetro"
            )

    vistos = set()
    for elemento in raiz.iter():
        ident = elemento.get("id")
        if ident and ident in vistos:
            achados.append(f"{vista}: id duplicado '{ident}'")
        elif ident:
            vistos.add(ident)

    for elemento in raiz.iter():
        if elemento.tag not in (f"{SVG}text", f"{SVG}tspan"):
            continue
        familia = (elemento.get("font-family") or "").strip("\"'")
        if familia and familia not in FONTES:
            achados.append(f"{vista}: fonte '{familia}' nao esta na lista do Fritzing")

    return achados


def _conferir_conectores(raiz_fzp, arquivo, tag, vista):
    achados = []
    raiz_svg = ElementTree.parse(arquivo).getroot()
    por_id = {e.get("id"): e for e in raiz_svg.iter() if e.get("id")}

    centros = []
    for conector in raiz_fzp.iter("connector"):
        vista_no = conector.find(f"views/{tag}")
        for p in [] if vista_no is None else list(vista_no):
            alvo = p.get("svgId")
            elemento = por_id.get(alvo)
            if elemento is None:
                achados.append(f"{vista}: o FZP cita {alvo}, que nao existe no SVG")
                continue
            if _invisivel(elemento):
                achados.append(f"{vista}: {alvo} nao desenha nada; conector invisivel")
            if vista == "breadboard":
                x = _numero(elemento, "x", "cx")
                if x is not None:
                    centros.append(x)

    achados += _conferir_passo(centros, vista)
    return achados


def _invisivel(elemento):
    preenchimento = elemento.get("fill", "")
    traco = elemento.get("stroke", "")
    if preenchimento not in ("", "none"):
        return False
    if traco not in ("", "none") and float(elemento.get("stroke-width", 0) or 0) > 0:
        return False
    return True


def _numero(elemento, *atributos):
    for atributo in atributos:
        valor = elemento.get(atributo)
        if valor is not None:
            try:
                return float(valor)
            except ValueError:
                return None
    return None


def _conferir_passo(centros, vista):
    """Os conectores da barra ficam num passo constante de 0,1 polegada.

    Peca com ANT tem um conector solto fora da barra, entao a conferencia olha os
    espacamentos consecutivos e so reclama do que esta perto de um passo mas errado.
    """
    achados = []
    centros = sorted(centros)
    for anterior, seguinte in zip(centros, centros[1:]):
        distancia = seguinte - anterior
        if distancia > PASSO * 1.5:
            continue  # conector solto, fora da barra
        if abs(distancia - PASSO) > FOLGA:
            achados.append(
                f"{vista}: passo de {distancia:.2f} u entre pinos, esperado {PASSO}"
            )
    return achados
```

- [ ] **Passo 4: escrever um `fritzing/empacotar.py` mínimo, só para o teste coletar**

O arquivo completo é a Task 4. Aqui entra apenas o cabeçalho e `def empacotar(...)`
levantando `NotImplementedError`, porque o `import empacotar` no topo do arquivo de
teste impede a coleta enquanto o módulo não existir.

- [ ] **Passo 5: rodar e confirmar que passa**

Comando: `cd /c/HENRIQUE/Claude/Arduino && python -m pytest testes/test_fritzing.py -q`
Esperado: `12 passed`.

- [ ] **Passo 6: rodar a suíte inteira, para não ter quebrado nada**

Comando: `cd /c/HENRIQUE/Claude/Arduino && python -m pytest -q`
Esperado: `30 passed` (os 18 que já existiam mais os 12 novos).

- [ ] **Passo 7: commit**

```bash
git add fritzing/validar.py fritzing/empacotar.py testes/test_fritzing.py
git commit -m "Confere peca Fritzing por leitura, antes de abrir o programa"
```

---

## Task 2: transmissor FS1000A

**Arquivos:**
- Criar: `fritzing/rf433-tx/part.fzp`
- Criar: `fritzing/rf433-tx/svg/breadboard/rf433_tx_breadboard.svg`
- Criar: `fritzing/rf433-tx/svg/schematic/rf433_tx_schematic.svg`
- Criar: `fritzing/rf433-tx/svg/pcb/rf433_tx_pcb.svg`
- Criar: `fritzing/rf433-tx/svg/icon/rf433_tx_icon.svg`
- Modificar: `testes/test_fritzing.py`

### Geometria — breadboard (72 u/pol, placa de 19 × 19 mm)

| o quê | valor |
|---|---|
| `<svg width>` / `height` | `0.74803in` / `0.88976in` |
| `viewBox` | `0 0 53.858 64.063` |
| placa | x 0…53,858, y 0…53,858, verde `#1f7a34`, cantos `rx="1.5"` |
| corpo preto da barra | x 6,236…27,836, y 46,771…53,858, `#1a1a1a` |
| pinos, largura × altura | 1,814 × 10,205, `#8c8c8c`, começando em y 53,858 |
| centro dos pinos | 9,836 · 17,036 · 24,236 (passo 7,2 exato) |
| pad ANT | círculo em (45,354; 14,173), r 2,6, `#c0c0c0` com furo `#1f7a34` r 1,2 |

Ordem dos pinos, esquerda para a direita: **DATA · VCC · GND**. Confirmado na plaquinha
em 06/09/2026; a serigrafia do transmissor fica no lado dos componentes, então não há
espelhamento.

Conectores, com x já descontado de metade da largura do pino:

```xml
<rect id="connector0pin" x="8.929"  y="53.858" width="1.814" height="10.205" fill="#8c8c8c" stroke="none"/>
<rect id="connector1pin" x="16.129" y="53.858" width="1.814" height="10.205" fill="#8c8c8c" stroke="none"/>
<rect id="connector2pin" x="23.329" y="53.858" width="1.814" height="10.205" fill="#8c8c8c" stroke="none"/>
```

O conector 3 é o ANT, o próprio círculo prateado do pad, com `id="connector3pin"`.

Ornamento, dentro do `<g id="breadboard">` e sem id nenhum, para não colidir: ressonador
SAW como cápsula prateada deitada de 7 × 3,5 mm no alto à direita, bobina de cobre
`#b87333` de cinco voltas ao centro, SOT-23 preto, três ou quatro passivos SMD em
`#4d4d4d` e `#c7b299`, e os textos `FS1000A`, `ANT`, `DATA`, `VCC`, `GND` em branco,
`font-family="Noto Sans"`, `font-size="3.2"`.

### Geometria — schematic (72 u/pol)

`width="1.1in" height="0.9in" viewBox="0 0 79.2 64.8"`. Caixa em x 14,4…64,8, y 14,4…50,4,
`fill="none" stroke="#000000" stroke-width="0.9"`.

```xml
<rect id="connector0pin" connectorName="DATA" x="0"     y="28.45" width="14.4" height="0.7"  fill="#787878" stroke="none"/>
<rect id="connector1pin" connectorName="VCC"  x="35.65" y="0"     width="0.7"  height="14.4" fill="#787878" stroke="none"/>
<rect id="connector2pin" connectorName="GND"  x="35.65" y="50.4"  width="0.7"  height="14.4" fill="#787878" stroke="none"/>
<rect id="connector3pin" connectorName="ANT"  x="64.8"  y="28.45" width="14.4" height="0.7"  fill="#787878" stroke="none"/>
```

Todos os terminais caem em múltiplo de 7,2, que é a grade de 0,1 polegada do esquema.
Dentro da caixa: `FS1000A` e `433 MHz TX` centralizados, e os rótulos `DATA`, `VCC`,
`GND`, `ANT` junto das bordas. Fonte `Noto Sans`, tamanho 5 nos rótulos e 6 no título.

### Geometria — pcb (1000 u/pol, placa de 19 × 19 mm)

`width="0.74803in" height="0.74803in" viewBox="0 0 748.03 748.03"`.

| conector | cx | cy |
|---|---|---|
| connector0 DATA | 136,6 | 696,8 |
| connector1 VCC | 236,6 | 696,8 |
| connector2 GND | 336,6 | 696,8 |
| connector3 ANT | 629,9 | 196,9 |

Todos `r="27.5" stroke-width="20" fill="none" stroke="rgb(255, 191, 0)"`, dentro de
`<g id="copper1"><g id="copper0">…</g></g>`. Em `<g id="silkscreen">`, o contorno da placa
`fill="none" stroke="#f0f0f0" stroke-width="10"` e o texto `FS1000A` em
`font-family="OCR-Fritzing-mono"`.

### Geometria — icon (72 u/pol)

`width="0.32in" height="0.32in" viewBox="0 0 23.04 23.04"`, camada `<g id="icon">`.
A placa verde ocupando tudo, com o SAW e a bobina simplificados e o texto `433`. Sem
conector: ícone não tem.

### FZP

`moduleId="rf433-tx-fs1000a-hwlab"`. Propriedades, nenhuma vazia:

| propriedade | valor |
|---|---|
| family | `rf module 433mhz` |
| variant | `transmitter FS1000A` |
| part number | `MX-FS-03V` |
| frequency | `433 MHz` |
| voltage | `3.5-12V` |
| modulation | `ASK/OOK` |

A `family` é a mesma do receptor de propósito: com isso o Inspector do Fritzing oferece
trocar uma peça pela outra na lista de variantes.

Conectores `type="male"`, com `<p svgId="connectorNpin" layer="…"/>` em cada vista e
**sem `terminalId`**. No PCB, duas linhas por conector, `layer="copper0"` e
`layer="copper1"`, com o mesmo `svgId`. `<buses/>` vazio: no transmissor não há pino
repetido.

- [ ] **Passo 1: escrever o teste que falha**

Acrescentar ao fim de `testes/test_fritzing.py`:

```python
def ordem_da_barra(pasta, svg, conectores):
    """Nomes dos pinos da barra, da esquerda para a direita na vista de cima."""
    fzp = ElementTree.parse(RAIZ / "fritzing" / pasta / "part.fzp").getroot()
    bb = ElementTree.parse(RAIZ / "fritzing" / pasta / "svg" / "breadboard" / svg).getroot()
    x_de = {e.get("id"): float(e.get("x")) for e in bb.iter() if e.get("x") and e.get("id")}
    nome_de = {c.get("id"): c.get("name") for c in fzp.iter("connector")}
    return [nome_de[c] for c in sorted(conectores, key=lambda c: x_de[c + "pin"])]


def test_transmissor_nao_tem_queixa():
    assert validar.problemas(RAIZ / "fritzing" / "rf433-tx") == []


def test_transmissor_tem_a_ordem_de_pinos_conferida_na_plaquinha():
    """DATA VCC GND da esquerda para a direita, vista de cima. Trocar isto
    liga alimentacao no lugar errado."""
    ordem = ordem_da_barra(
        "rf433-tx",
        "rf433_tx_breadboard.svg",
        ["connector0", "connector1", "connector2"],
    )
    assert ordem == ["DATA", "VCC", "GND"]
```

- [ ] **Passo 2: rodar e confirmar que falha**

Comando: `cd /c/HENRIQUE/Claude/Arduino && python -m pytest testes/test_fritzing.py -q`
Esperado: FAIL, `nao tem part.fzp`.

- [ ] **Passo 3: escrever os quatro SVGs e o FZP** conforme as tabelas acima.

- [ ] **Passo 4: rodar e confirmar que passa**

Comando: `cd /c/HENRIQUE/Claude/Arduino && python -m pytest testes/test_fritzing.py -q`
Esperado: `14 passed`.

- [ ] **Passo 5: rodar o conferidor do próprio Fritzing**

```bash
python "$HOME/AppData/Local/Programs/Fritzing/fritzing-parts/fzp_checker.py" fritzing/rf433-tx/part.fzp
```
Esperado: zero erro. Aviso sobre arquivo fora da árvore de peças do Fritzing é esperado
e não conta.

- [ ] **Passo 6: commit**

```bash
git add fritzing/rf433-tx testes/test_fritzing.py
git commit -m "Desenha a peca do transmissor FS1000A"
```

---

## Task 3: receptor MX-05V

**Arquivos:**
- Criar: `fritzing/rf433-rx/part.fzp`
- Criar: `fritzing/rf433-rx/svg/breadboard/rf433_rx_breadboard.svg`
- Criar: `fritzing/rf433-rx/svg/schematic/rf433_rx_schematic.svg`
- Criar: `fritzing/rf433-rx/svg/pcb/rf433_rx_pcb.svg`
- Criar: `fritzing/rf433-rx/svg/icon/rf433_rx_icon.svg`
- Modificar: `testes/test_fritzing.py`

### Geometria — breadboard (72 u/pol, placa de 30 × 14 mm)

| o quê | valor |
|---|---|
| `<svg width>` / `height` | `1.18110in` / `0.69292in` |
| `viewBox` | `0 0 85.039 49.890` |
| placa | x 0…85,039, y 0…39,685, `#1f7a34`, `rx="1.5"` |
| corpo preto da barra | x 5,669…34,469, y 32,598…39,685 |
| centro dos pinos | 9,269 · 16,469 · 23,669 · 30,869 |
| pad ANT | círculo em (77,953; 11,339), r 2,6 |

Ordem, esquerda para a direita, vista de cima: **VCC · DATA · DATA · GND**. É o espelho da
serigrafia, que fica no verso — vista de baixo é GND · DATA · DATA · VCC. Confirmado por
Henrique em 06/09/2026.

```xml
<rect id="connector0pin" x="8.362"  y="39.685" width="1.814" height="10.205" fill="#8c8c8c" stroke="none"/>
<rect id="connector1pin" x="15.562" y="39.685" width="1.814" height="10.205" fill="#8c8c8c" stroke="none"/>
<rect id="connector2pin" x="22.762" y="39.685" width="1.814" height="10.205" fill="#8c8c8c" stroke="none"/>
<rect id="connector3pin" x="29.962" y="39.685" width="1.814" height="10.205" fill="#8c8c8c" stroke="none"/>
```

Ornamento: SOIC-8 preto de 5 × 4 mm à esquerda do centro, trimpot azul-claro `#7fd4d4` de
4,5 × 4,5 mm com fenda de ajuste dourada ao centro, bobina de cobre `#b87333` na
extremidade direita, passivos SMD, e os textos `MX-05V`, `VCC`, `DATA`, `DATA`, `GND`,
`ANT` em `Noto Sans` tamanho 3,2.

A barra de pinos fica na extremidade oposta à da bobina, que é o que as fotos mostram.
Se a placa real estiver espelhada em relação a isso, o conserto é trocar o sinal do x do
ornamento — a ordem elétrica dos pinos não muda.

### Geometria — schematic (72 u/pol)

`width="1.1in" height="1.0in" viewBox="0 0 79.2 72"`. Caixa em x 14,4…64,8, y 14,4…57,6.

```xml
<rect id="connector0pin" connectorName="VCC"   x="35.65" y="0"     width="0.7"  height="14.4" fill="#787878" stroke="none"/>
<rect id="connector1pin" connectorName="DATA"  x="0"     y="28.45" width="14.4" height="0.7"  fill="#787878" stroke="none"/>
<rect id="connector2pin" connectorName="DATA2" x="0"     y="42.85" width="14.4" height="0.7"  fill="#787878" stroke="none"/>
<rect id="connector3pin" connectorName="GND"   x="35.65" y="57.6"  width="0.7"  height="14.4" fill="#787878" stroke="none"/>
<rect id="connector4pin" connectorName="ANT"   x="64.8"  y="35.65" width="14.4" height="0.7"  fill="#787878" stroke="none"/>
```

### Geometria — pcb (1000 u/pol, placa de 30 × 14 mm)

`width="1.18110in" height="0.55118in" viewBox="0 0 1181.10 551.18"`.

| conector | cx | cy |
|---|---|---|
| connector0 VCC | 128,7 | 500,0 |
| connector1 DATA | 228,7 | 500,0 |
| connector2 DATA2 | 328,7 | 500,0 |
| connector3 GND | 428,7 | 500,0 |
| connector4 ANT | 1082,7 | 157,5 |

### FZP

`moduleId="rf433-rx-mx05v-hwlab"`, mesma `family` `rf module 433mhz`,
`variant` `receiver MX-05V`, `part number` `MX-05V`, `frequency` `433 MHz`,
`voltage` `5V`, `sensitivity` `-105 dBm`.

**O barramento é a parte que não pode faltar.** Os dois DATA são o mesmo ponto na placa;
sem declarar, o Fritzing os trata como pinos independentes e acusa conexão faltando quando
só um é usado:

```xml
<buses>
 <bus id="dados">
  <nodeMember connectorId="connector1"/>
  <nodeMember connectorId="connector2"/>
 </bus>
</buses>
```

- [ ] **Passo 1: escrever os testes que falham**

```python
def test_receptor_nao_tem_queixa():
    assert validar.problemas(RAIZ / "fritzing" / "rf433-rx") == []


def test_receptor_tem_a_ordem_espelhada_da_serigrafia():
    """De cima e VCC DATA DATA GND. A serigrafia fica no verso, e ler o verso
    sem espelhar troca alimentacao com terra."""
    ordem = ordem_da_barra(
        "rf433-rx",
        "rf433_rx_breadboard.svg",
        ["connector0", "connector1", "connector2", "connector3"],
    )
    assert ordem == ["VCC", "DATA", "DATA2", "GND"]


def test_receptor_liga_os_dois_data_em_barramento():
    """Sao o mesmo ponto na placa. Sem o barramento o Fritzing acusa
    conexao faltando quando so um DATA e usado."""
    fzp = ElementTree.parse(RAIZ / "fritzing" / "rf433-rx" / "part.fzp").getroot()
    membros = {m.get("connectorId") for m in fzp.iter("nodeMember")}
    assert membros == {"connector1", "connector2"}
```

- [ ] **Passo 2: rodar e confirmar que falha**

Comando: `cd /c/HENRIQUE/Claude/Arduino && python -m pytest testes/test_fritzing.py -q`
Esperado: FAIL, `nao tem part.fzp`.

- [ ] **Passo 3: escrever os quatro SVGs e o FZP** conforme as tabelas acima.

- [ ] **Passo 4: rodar e confirmar que passa**

Comando: `cd /c/HENRIQUE/Claude/Arduino && python -m pytest testes/test_fritzing.py -q`
Esperado: `17 passed`.

- [ ] **Passo 5: rodar o conferidor do Fritzing**

```bash
python "$HOME/AppData/Local/Programs/Fritzing/fritzing-parts/fzp_checker.py" fritzing/rf433-rx/part.fzp
```
Esperado: zero erro.

- [ ] **Passo 6: commit**

```bash
git add fritzing/rf433-rx testes/test_fritzing.py
git commit -m "Desenha a peca do receptor MX-05V, com os dois DATA em barramento"
```

---

## Task 4: empacotador

**Arquivos:**
- Modificar: `fritzing/empacotar.py`
- Modificar: `testes/test_fritzing.py`

O `.fzpz` é um zip de arquivos planos: `part.<moduleid>.fzp` e `svg.<vista>.<nome>.svg`.
O FZP dentro do zip continua citando `breadboard/nome.svg` — quem traduz é o Fritzing, na
importação. Por isso o empacotador só renomeia na hora de zipar e não reescreve o XML.

- [ ] **Passo 1: escrever os testes que falham**

```python
def test_empacota_com_os_nomes_planos_que_o_fritzing_espera(tmp_path, peca):
    destino = empacotar.empacotar(peca, tmp_path / "Peca.fzpz")
    nomes = set(zipfile.ZipFile(destino).namelist())
    assert nomes == {
        "part.peca-teste.fzp",
        "svg.breadboard.bb.svg",
        "svg.schematic.sc.svg",
        "svg.pcb.pcb.svg",
        "svg.icon.ic.svg",
    }


def test_recusa_empacotar_peca_com_problema(tmp_path, peca):
    (peca / "svg" / "icon" / "ic.svg").unlink()
    with pytest.raises(ValueError, match="icon/ic.svg"):
        empacotar.empacotar(peca, tmp_path / "Peca.fzpz")
```

- [ ] **Passo 2: rodar e confirmar que falha**

Comando: `cd /c/HENRIQUE/Claude/Arduino && python -m pytest testes/test_fritzing.py -q`
Esperado: FAIL, `NotImplementedError`.

- [ ] **Passo 3: escrever `fritzing/empacotar.py` por inteiro**

```python
"""Zipa uma pasta de peca no .fzpz que o Fritzing importa por Arquivo > Abrir.

O .fzpz e um zip plano: part.<moduleid>.fzp e svg.<vista>.<nome>.svg. O FZP la dentro
continua citando 'breadboard/nome.svg'; quem faz a traducao e o Fritzing na importacao,
entao aqui so o nome do arquivo muda.

  python fritzing/empacotar.py          empacota as duas pecas em fritzing/dist/
"""
import sys
import zipfile
from pathlib import Path
from xml.etree import ElementTree

import validar

PASTA = Path(__file__).resolve().parent

PECAS = {
    "rf433-tx": "RF433-TX-FS1000A.fzpz",
    "rf433-rx": "RF433-RX-MX05V.fzpz",
}


def empacotar(pasta, destino):
    """Zipa `pasta` em `destino`. Levanta ValueError se a peca tiver problema."""
    pasta = Path(pasta)
    achados = validar.problemas(pasta)
    if achados:
        raise ValueError(f"{pasta.name} tem problema: " + "; ".join(achados))

    fzp = pasta / "part.fzp"
    module_id = ElementTree.parse(fzp).getroot().get("moduleId")

    destino = Path(destino)
    destino.parent.mkdir(parents=True, exist_ok=True)
    with zipfile.ZipFile(destino, "w", zipfile.ZIP_DEFLATED) as zip_:
        zip_.write(fzp, f"part.{module_id}.fzp")
        for arquivo in sorted((pasta / "svg").rglob("*.svg")):
            vista = arquivo.parent.name
            zip_.write(arquivo, f"svg.{vista}.{arquivo.name}")
    return destino


def main():
    for pasta, nome in PECAS.items():
        destino = empacotar(PASTA / pasta, PASTA / "dist" / nome)
        print(destino.relative_to(PASTA.parent))


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Passo 4: rodar e confirmar que passa**

Comando: `cd /c/HENRIQUE/Claude/Arduino && python -m pytest testes/test_fritzing.py -q`
Esperado: `19 passed`.

- [ ] **Passo 5: gerar os dois .fzpz de verdade**

Comando: `cd /c/HENRIQUE/Claude/Arduino && python fritzing/empacotar.py`
Esperado: duas linhas, `fritzing/dist/RF433-TX-FS1000A.fzpz` e
`fritzing/dist/RF433-RX-MX05V.fzpz`.

- [ ] **Passo 6: commit**

```bash
git add fritzing/empacotar.py fritzing/dist testes/test_fritzing.py
git commit -m "Empacota as pecas no .fzpz que o Fritzing importa"
```

---

## Task 5: conferência visual

**Arquivos:**
- Criar: `fritzing/previa.html`

Nenhum teste pega desenho torto. Esta tarefa existe porque o desenho é o produto, e ele
só se confere olhando.

- [ ] **Passo 1: escrever `fritzing/previa.html`**

Uma página que embute as oito SVGs por `<img>`, agrupadas por peça, cada uma rotulada com
a vista e o tamanho físico declarado. Fundo cinza-médio `#8a8a8a`, para o verde da placa
e o branco da serigrafia aparecerem os dois. As vistas de breadboard e ícone ampliadas
4×, o esquema e o PCB 2,5×, senão a peça sai do tamanho de uma unha na tela.

- [ ] **Passo 2: renderizar**

```bash
pwsh ferramentas/previa.ps1 -Arquivo fritzing/previa.html -Largura 1100 -Altura 1400 -Destino previa-fritzing.png -Inteira
```
Esperado: `previa-fritzing.png` gravado, sem erro de console.
O `.gitignore` já ignora `previa-*.png`; a imagem é para olhar, não para versionar.

- [ ] **Passo 3: olhar a imagem e conferir**

- os três pinos do TX e os quatro do RX estão alinhados e igualmente espaçados;
- a ordem dos rótulos bate com a tabela de pinagem;
- nenhum texto sai da placa nem se sobrepõe a componente;
- o esquemático tem os pinos saindo da caixa, sem pino solto no vazio;
- o PCB mostra os furos dentro do contorno da placa.

- [ ] **Passo 4: mostrar a imagem a Henrique** antes de dizer que está pronto.

- [ ] **Passo 5: commit**

```bash
git add fritzing/previa.html
git commit -m "Folha de contato das oito SVGs, para conferir o desenho a olho"
```

---

## Task 6: instalação, README e inventário

**Arquivos:**
- Criar: `fritzing/README.md`
- Modificar: `componentes/comunicacao.yaml`
- Modificar: `CLAUDE.md`

- [ ] **Passo 1: escrever `fritzing/README.md`**

Como instalar: no Fritzing, `Arquivo > Abrir` apontando para o `.fzpz`; a peça entra no
bin *Minhas Peças* e fica em `Documentos/Fritzing/parts/user`. Como regerar depois de
mexer nos SVGs: `python fritzing/empacotar.py`. E o aviso de que **reimportar não
atualiza peça já usada num sketch** — o Fritzing guarda cópia da peça dentro do `.fzz`,
então sketch antigo continua com o desenho velho.

- [ ] **Passo 2: acrescentar a nota no inventário**

Em `componentes/comunicacao.yaml`, uma linha ao fim de `notas` de `rf433-tx` e de
`rf433-rx`: `Peca Fritzing propria em fritzing/rf433-tx/ (ou -rx).` Uma linha é uma
afirmação completa — a página renderiza com `white-space: pre-line`.

- [ ] **Passo 3: regerar a página**

Comando: `cd /c/HENRIQUE/Claude/Arduino && python ferramentas/gerar-pagina.py`
Esperado: `README.md` e `index.html` reescritos. Nunca editar os dois à mão.

- [ ] **Passo 4: registrar em `CLAUDE.md`**

Um parágrafo curto dizendo que `fritzing/` guarda peças próprias, que `empacotar.py` é
quem gera o `.fzpz`, e que a pinagem do receptor é espelhada entre as duas faces.

- [ ] **Passo 5: rodar a suíte inteira**

Comando: `cd /c/HENRIQUE/Claude/Arduino && python -m pytest -q`
Esperado: `37 passed`.

- [ ] **Passo 6: commit e push**

```bash
git add -A
git commit -m "Documenta as pecas Fritzing e liga ao inventario"
git push
```

---

## Conferência do plano contra o spec

- arte fiel à foto → Tasks 2 e 3, seções de ornamento
- pinagem TX e RX → Tasks 2 e 3, com teste dedicado para cada ordem
- barramento dos dois DATA → Task 3, com teste
- viewBox, unidade física, fontes, sem `terminalId`, ids únicos, `copper0` em `copper1`,
  propriedade não vazia → Task 1, um teste por regra
- as quatro vistas de cada peça → Tasks 2 e 3
- estrutura de pastas e `.fzpz` → Task 4
- verificação pelo `fzp_checker.py` → Tasks 2 e 3, passo 5
- folha de contato pelo `previa.ps1` → Task 5
- encaixe no passo de 0,1" conferido por Henrique no Fritzing → Task 5, passo 4

O spec diz que a posição da barra ao longo da borda fica para conferência visual. Está na
Task 3, na observação sobre espelhar o ornamento, e na Task 5.
